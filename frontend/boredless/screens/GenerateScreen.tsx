import { View, Text } from 'react-native';
import { styles } from './GenerateScreen.styles';

export const GenerateScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Generate</Text>
    </View>
  );
}; 